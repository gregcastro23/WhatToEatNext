{
  "name": "Parallel Development Accelerator",
  "description": "Orchestrates multiple improvement processes in parallel while maintaining Docker robustness and maximizing development velocity",
  "trigger": {
    "type": "onInterval",
    "interval": 180000
  },
  "condition": {
    "script": "const fs = require('fs'); const path = require('path'); return fs.existsSync(path.join(process.cwd(), 'src')) && fs.existsSync(path.join(process.cwd(), 'Dockerfile'));"
  },
  "actions": [
    {
      "type": "runScript",
      "script": "src/services/campaign/parallelDevelopmentAccelerator.js",
      "parameters": {
        "enableDockerOptimization": true,
        "enableParallelProcessing": true,
        "enableContinuousImprovement": true,
        "maxParallelTasks": 4,
        "dockerHealthChecks": true,
        "buildValidation": true
      }
    }
  ],
  "settings": {
    "autoExecute": true,
    "runInBackground": true,
    "priority": "high",
    "maxExecutionsPerHour": 20,
    "retryOnFailure": true,
    "maxRetries": 2
  }
}
