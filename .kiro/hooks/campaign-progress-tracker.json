{
  "name": "Campaign Progress Tracker",
  "description": "Monitors campaign progress and triggers next phases automatically",
  "trigger": {
    "type": "onInterval",
    "interval": 300000
  },
  "condition": {
    "script": "const fs = require('fs'); const path = require('path'); const campaignDir = path.join(process.cwd(), '.kiro', 'campaign-triggers'); return fs.existsSync(campaignDir) && fs.readdirSync(campaignDir).length > 0;"
  },
  "actions": [
    {
      "type": "runScript",
      "script": "src/services/campaign/campaignProgressTracker.js",
      "parameters": {
        "autoAdvancePhases": true,
        "safetyChecks": true,
        "reportProgress": true
      }
    }
  ],
  "settings": {
    "autoExecute": true,
    "runInBackground": true,
    "maxExecutionsPerHour": 12
  }
}
