{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf .next .vercel/output .tsbuildinfo && yarn build",
  "installCommand": "rm -rf node_modules/.cache .yarn/cache .yarn/install-state.gz && yarn install --network-timeout 300000 --check-files",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1",
    "YARN_ENABLE_IMMUTABLE_INSTALLS": "false",
    "NODE_OPTIONS": "--max-old-space-size=4096",
    "YARN_HTTP_TIMEOUT": "300000",
    "YARN_NETWORK_TIMEOUT": "300000"
  }
}
