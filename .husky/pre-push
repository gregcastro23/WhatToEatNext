#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Add common PATH locations for yarn
export PATH="$PATH:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"

which yarn && /usr/local/bin/yarn lint && /usr/local/bin/yarn build
