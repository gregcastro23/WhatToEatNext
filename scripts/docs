#!/bin/bash

# Short alias for doc-update.sh
# Allows running ./scripts/docs instead of ./scripts/doc-update.sh

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/doc-update.sh" "$@"