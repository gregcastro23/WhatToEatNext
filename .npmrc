# Force users to use yarn instead of npm
engine-strict=true

# Network retry configuration for transient registry failures
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
